import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const Saved = () => {
  return (
    <View className="flex-1 items-center justify-center bg-dark-600">
        <Text className="text-2xl text-white">Saved Screen</Text>
    </View>
  )
}

export default Saved

const styles = StyleSheet.create({})